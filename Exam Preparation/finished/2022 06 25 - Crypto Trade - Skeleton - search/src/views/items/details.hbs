

{{!-- 
//details: details_hbs, hb_if_else
//copy/paste the main or the section html here:

//fix href="#" :                      href="/items/{{_id}}/edit"
//fix href="#" :                      href="/items/{{_id}}/delete"
//fix href="#" :                      href="/items/{{_id}}/like"
//replace src with image shape:       src="{{image}}"
//Add likes count:                    {{likesCount}}
//Add the postOwner email:            {{postOwner.email}}
//Add the postOwner username:         {{postOwner.username}}

//Add [] likedUsersUsernames          {{likedUsersUsernamesString}}
//Add [] likedUsersIds                {{likedUsersIds}}
//Add [] likedUsersEmails             {{likedUsersEmailsString}}
//replace text with variables:        {{name}}
//copy shape here from create.hbs:
//name
//image
//price
//description
//method
//buyCrypto
//owner


//--}}

<main>
    <section id="details-info">
        <h1>Details</h1>
        <div class="coin-image">
            <img src="{{image}}">
        </div>
        <div class="coin-info">
            <div class="coin-text">
                <h1 id="name">{{name}}</h1>
                <h3 id="payment">Payment method: {{method}}</h3>
                <p id="price"><span>Price: ${{price}}</span></p>
                <p id="description">{{description}}</p>
            </div>
                {{#if user}}
                  <!--If there is user logged in: <div class="buttons"> -->
                  <!--!-->
                  <div class="product-btn">
                      {{#if isOwner}}
                        <!--If user is owner: Edit,Delete-->
                        <!--!-->
                        <!--!-->
                        <div class="author">
                           <a href="/items/{{_id}}/edit" class="btn-edit">Edit</a>
                           <a href="/items/{{_id}}/delete" class="btn-delete">Delete</a>
                       </div> 
                      {{else}}
                          {{#if isLiked}}
                            <!--If user liked-->
                            <!--!-->
                            <p class="buy-message">You already bought these crypto coins.</p>
                          {{else}}
                            <!--If user didn't liked-->
                            <!--!-->
                            <a href="/items/{{_id}}/like" class="btn-buy">Buy</a>
                          {{/if}}
                      {{/if}}
                  <!--!-->
                  </div>
                {{/if}}              
        </div>
    </section>
</main>

