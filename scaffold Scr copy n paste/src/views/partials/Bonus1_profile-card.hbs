


{{!-- 
//Copy/Paste the loop element in dashboard.html
//fix href="#" :                href="/items/{{_id}}/details"
//replace src with image shape: src="{{image}}"
//replace text with variables:  {{name}}
//Add likes count:              {{voted.length}}


--}}






















{{!--//Example1--}}
{{!--//Delete later--}}
<div class="my-card">
    <div class="card-header">
        <img src="{{image}}" />
    </div>
    <div class="card-body">
        <span class="tag tag-teal">Species: {{species}}</span>
        <h4>
            Name: {{name}}
        </h4>
        <div class="user">
            <div class="user-info">
                <h5>Author: {{user}}</h5>
                <small>Rating of votes: {{collectionProperty.length}}</small>
                <a href="/items/{{_id}}/details" class="details_btn">Details</a>
            </div>
        </div>
    </div>
</div>